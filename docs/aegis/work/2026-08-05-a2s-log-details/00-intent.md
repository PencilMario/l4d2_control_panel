# Intent

Add useful per-source context to sampled A2S protection logs and make the existing blacklist-drop counter reflect the installed sampled drop-chain rules.

Non-goals: exact attack packet totals, configurable sampling, IPv6, firewall policy changes, new privileges, or manual blacklist management.
