# Checkpoint

## TodoCheckpointDraft

- 当前任务：修复并复验 Markdown 文档大纲。
- 已完成：确认用户批准独立阅读态与引入依赖；创建干净隔离工作树；读取 API/UI/测试/CSS 基线；写入本任务的意图、基线和计划；`CrashReportsPage` 基线 2/2 通过；新增测试在入口缺失时按预期失败；安全阅读组件、入口和 scoped 响应式样式已接入；初始目标测试 3/3 通过；全量前端回归 20 文件/211 测试通过；生产构建通过。
- 活动切片：新增 HTML 安全和一级标题结构断言后，已发现重复 H1；保留 Markdown 报告的唯一 H1，降低阅读器 chrome 的标题语义。
- 下一步：重新跑目标和全量测试、构建及差异检查。

## DriftCheckDraft

- 范围：仍限于 AI 分析展示层。
- 兼容性：不改 API、后端、AI 调用或普通崩溃详情工作流。
- 结论：continue。
