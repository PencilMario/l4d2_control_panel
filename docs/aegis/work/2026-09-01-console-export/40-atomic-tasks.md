# 控制台文本导出原子任务记录

- [x] 在现有控制台旅程中验证空内容禁用、输出后启用、Blob 内容、下载文件名和 Object URL 释放。
- [x] 新增 `consoleExport.ts`，实现安全文件名和本地 `.txt` 下载；在 `Terminal` 标题栏接入导出按钮。
- [x] 增加非法文件名与空名称边界测试。
- [x] 完成前端全量测试、Go 全量测试和生产构建验证。

## Drift Check

- 当前改动仍服务于“将实例控制台当前文本导出为 TXT”和“缓存上限提高到 8192 行”。
- 未增加后端 API、持久化数据或新的控制台数据 owner。
- 未修改根目录已有的 Accelerator 改动和日志导出目录。
- 1000 行旧默认边界已由 8192 行边界取代；后端 1 MiB 字节上限保留。
