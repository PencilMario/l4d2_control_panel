# Verification Evidence

## Targeted checks

- `go test ./internal/crashreports -run TestReceiveUsesGameContainerResolvedAtPreSubmit -count=1`
  - Red before the fix: the report used `new-game-container`.
  - Green after the fix: the report used `old-game-container` and resolved once.
- `go test ./internal/crashartifacts ./internal/systemlibs -run "TestPreparerContinuesAfterCandidateDebugIdentifierMismatch|TestPreparerRejectsDebugIdentifierMismatchWithoutSavingBinary|TestIsAllowed|TestCandidatePaths" -count=1`
  - Confirms candidate fallback, mismatch rejection, fixed path allowlist, and architecture-specific candidates.

## Final checks

- `go test ./...`
  - Passed for all packages; packages without tests reported `[no test files]`.
- `go build -o NUL ./cmd/panel`
  - Passed.
- `go build -o NUL ./cmd/socket-proxy`
  - Passed.
- `git diff --check`
  - Passed with no whitespace errors.

## Scope and residual risk

The source container is captured at pre-submit and only managed game containers matching the report instance are eligible. Archive reads are bounded and path-restricted; extraction remains best effort. The full repository test/build gate is run after this record is updated.

The worktree still contains an untracked `panel.exe` generated by an earlier local build. The cleanup command was rejected by the host command policy, so it was left untouched; it is not part of the source changes. The existing user modification to `web/public/vpk-cleaner.wasm` was also left untouched.
