# Task Intent: 后台任务强制停止

- 用户价值：管理员可以终止卡住或不再需要的执行中维护任务，并在任务记录中看到明确的中断结果。
- 范围：Manager 运行时取消、认证 HTTP 取消接口、任务页确认按钮、保留既有 CSS 布局、两个生产节点部署。
- 非目标：排队任务取消、任务重试/删除、数据库迁移、任务超时策略改造。
- 验收：取消命令真正传入任务上下文；终态为 `interrupted`；前端只给执行中任务显示并保护停止按钮；两个节点健康检查通过。
