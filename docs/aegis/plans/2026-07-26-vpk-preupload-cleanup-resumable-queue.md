# VPK Pre-upload Cleanup and Resumable Queue Plan

1. Extract and test the shared cleanup suffix policy.
2. Add upload recovery, cancellation, and offset HTTP behavior.
3. Add the Go WASM cleaner and worker build integration.
4. Add IndexedDB metadata and OPFS payload persistence.
5. Add the resumable queue with serial cleanup and two uploads.
6. Add the multi-file confirmation dialog and queue controls.
7. Add browser coverage and full regression verification.
